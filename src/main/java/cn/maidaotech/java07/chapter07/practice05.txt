调用wait()方法，所在线程会释放所持有的锁资源，进入等待状态，当被唤醒就会进入就绪状态来到就绪队列等待CPU分配时间片。
调用sleep()方法，所在线程不并不释放所持有的所资源，因为线程此时进入了休眠状态，休眠期满了之后就会进入就绪状态。

notify()方法，随机唤醒跟该线程等待同一资源的一个线程，调用该方法，并不会立即释放锁，而是等sychronized代码块执行完之后，才会释放所资源，被唤醒的线程才能拿到锁
notifyAll()方法，唤醒跟该线程等待同一资源的所有线程。。。